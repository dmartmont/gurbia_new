import React, { Component } from 'react';
import { AppRegistry } from 'react-native'

import App from './src/App';

export default class gurbia_new extends Component {
  render() {
    return (
      <App />
    );
  }
}

AppRegistry.registerComponent('gurbia_new', () => gurbia_new);
